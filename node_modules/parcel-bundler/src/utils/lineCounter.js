function lineCounter(string) {
  let lines = 1;
  if(string==undefined){
    lines = 1
  }
  else {
    for (let i = 0; i < string.length; i++) {
      if (string.charAt(i) === '\n') {
        lines++;
      }
    }
  }
  
  return lines;
}

module.exports = lineCounter;
